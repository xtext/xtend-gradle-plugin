ext.description = 'A library that provides build tooling not yet present in xtend-core'

dependencies {
  compile ('org.eclipse.xtend:org.eclipse.xtend.core:[2.5.4,)') {
    exclude group:'asm', module:'asm'
  }
  compile 'com.martiansoftware:nailgun-server:0.9.1'
}