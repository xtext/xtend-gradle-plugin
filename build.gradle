subprojects {
  apply plugin: 'groovy'
  apply plugin: 'eclipse'
  apply from: '../mavenRelease.gradle'
 
  group = "org.xtend"
  version = "0.0.7"
  
  repositories {
    mavenCentral()
  }
  
  dependencies {
    compile localGroovy()
  }
}